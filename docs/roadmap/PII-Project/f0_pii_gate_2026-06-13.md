# F0 — Scheduling Identity-Graph Deletion: zero-residue attestation

**Gate:** F0 (the unwaivable PII gate on scheduling v1's prod flag-flip, F1) —
`scheduling/docs/launch_blocker_remediation_2026-05-18.md` §N2.
**Verdict: PASS ✅** — a synthetic subject's *entire* scheduling footprint
(booking + reminder/attendance messages + scheduling-session + the **real
Google Calendar event** + the **real EventBridge schedule**) was hard-deleted
on a single email-keyed DSAR delete, with **zero residue** across every store.

## Custody line
- **Run by:** operator `chris@myrecruiter.ai` (DSAR exec role identity in the audit `caller_arn`).
- **When:** 2026-06-13 (UTC), staging acct **525409062831** only; synthetic data; prod (614) untouched — F1 remains a HARD STOP.
- **Pipeline under test:**
  - DSAR Lambda `picasso-pii-dsar-staging` CodeSha256 `j3dhxURU0OnebM2eriddy2Xqfoup+zLyJPYz6YZDaOU=` (deployed from lambda `feature/f0-scheduling-deletion` @ `981d819`; pre-F0 rollback CodeSha256 `ZMIRC0oQar4MlUYDpU8NXBUxX9JrCKwn6GyZ3stoevY=`).
  - IAM grants from picasso `feature/f0-scheduling-deletion` @ `2cb0be4` (applied to staging — `module.lambda_pii_dsar_staging[0].aws_iam_role_policy.dsar`, 0 add / 1 change / 0 destroy).
- **Advisory sign-off:** design vetted pre-code by `pii-data-lifecycle-advisor` + `privacy-data-governance-advisor` (6 blockers incorporated, F0 briefing §11); pre-commit adversarial review (code-reviewer + Security-Reviewer + test-engineer) — 3 security blockers fixed + 15 failure-path test gaps closed.
- **Unit evidence:** 282 tests pass (CI-green on lambda#326 `Python Tests — picasso_pii_dsar_staging`).

## Seed (synthetic; MYR384719; coordinator chris@myrecruiter.ai)
- subject `f0-attest-1781337785@example.invalid` (collision-proof `@example.invalid`)
- booking `booking#f0attest1781337785` (item_type=booking, attendee=subject) carrying a **real Google Calendar event** `cmg9skn31dhdpc52q7r5jp5fjo` created on the coordinator's calendar via the per-coordinator OAuth
- 2× `picasso-scheduled-messages` rows (reminder + attendance, `appointment_id == booking_id`)
- 1× `picasso-conversation-scheduling-session-staging` state row (`session_id`)
- 1× **real EventBridge schedule** `sched-attendance-booking-f0attest1781337785` (group `picasso-scheduling-reminders-staging`)

## Before → After
| Surface | Before | After |
|---|---|---|
| `picasso-booking-staging` (attendee match) | 1 | **0** |
| `picasso-scheduled-messages` (by-appointment) | 2 | **0** |
| `picasso-conversation-scheduling-session-staging` | 1 | **0** |
| **Google Calendar event** `cmg9skn3…` | present | **gone** |
| **EventBridge schedule** `sched-attendance-booking-f0attest…` | present | **gone** |
| **TOTAL live artifacts** | **6** | **0** |

## The run
- DSAR `delete` dry-run preview: `booking=1, scheduled-messages=2, scheduling-session=1` (calendar counted on real run only).
- DSAR `delete` non-dry-run: `status=partial` (the MFS chat surfaces are `skipped_no_subject` — this is a **scheduling-only subject** with no form / no subject-index entry, reached **email-keyed** per operator decision B / the F-DSAR4 class; the 4 scheduling surfaces ran clean with no errors). `rows_touched`: booking 1, calendar 1, scheduled-messages 2, scheduling-session 1.
- **Immutable audit trail** (`picasso-pii-dsar-audit`, dsar_id `f0-attest-1781337785-del`, 14 rows): `request_received → surface_walked:booking=completed → surface_walked:calendar=completed → surface_walked:scheduled-messages=completed → surface_walked:scheduling-session=completed → closed=partial`.

## Residue disclosures (stated, NOT claimed "zero")
Per the advisory-corrected design §11.2, "zero residue in the **live primary stores** at T+0" — with these named, justified retentions:
1. **DynamoDB PITR** — `picasso-booking-staging` + `picasso-conversation-scheduling-session-staging` have `point_in_time_recovery=true`; a deleted row remains restorable for **≤35 days** (catastrophic-recovery only; ages out). Matches `data-retention-strategy.md §4`; carried in `templates/dsar-response-delete.md`.
2. **Attendee's own calendar copy** — Google `events.delete` removes the **organizer** copy + sends cancellations but **cannot** reach an attendee's personal calendar. Disclosed in the delete-response template.
3. **Carve-outs** — `sms-consent` (TCPA proof, legal floor) and `sms-usage` (phone-keyed, Path-A) are deliberately NOT deleted; enumerated in the response template.
4. **Abandoned scheduling sessions** — a session abandoned *before* a booking (no booking_id, no form) has a state row with no email linkage and the table has **TTL DISABLED**; it is NOT email-reachable → tracked as the §12 retention-TTL decision (not in scope for this synthetic subject, who completed a booking).

## Verdict
**F0 zero-residue: PASS.** The DSAR pipeline hard-deletes a person's entire
scheduling footprint including the real Google Calendar event + EventBridge
schedule, auditor-confirmed zero residue in live primary stores, with the named
backup/external retentions disclosed (not hidden). **F0 is satisfied; the
scheduling v1 F1 prod flag-flip is unblocked — but remains operator-gated and a
HARD STOP for this session.** Merge of lambda#326 + picasso#570 awaits operator
go-ahead.
