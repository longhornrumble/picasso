# Privacy Risk Register (D5)

> **2026-05-22:** Master-plan milestone routing for every D5 row is maintained in [`MASTER_PROJECT_PLAN.md`](./MASTER_PROJECT_PLAN.md) §6 (closure-target column per row). The D5 register remains the authoritative risk inventory; the master plan routes each open row to its closure milestone (M1–M9) or explicit DEFERRED-with-named-trigger. Do not maintain a parallel routing column here — single source of truth lives in the master plan to prevent drift.

**Phase 0.5 deliverable D5.** Established 2026-05-20. Table form, **18 rows** (11 G-rows + 4 F-rows seeded at Phase 0.5 + 2 F-DSAR rows added 2026-05-20 from the DSAR Lambda item-1a audit + 1 F-DSAR row added 2026-05-21 from the 2-advisor audit of PR #135). Pre-seeded with the 11 gaps from the 2026-05-19 plan-gaps review (G-A..G-K) plus 4 new rows for the highest-priority D2+D3+D4-surfaced findings that don't map cleanly into a G-row. Each row carries a **named owner**; no unmitigated H/H row is ownerless.

> **Method:** consolidate prior enumeration. The 16 findings in D2+D3 + the Tier-vs-lifecycle gaps in D4 + the 11 gaps in the plan-gaps review + horizontal concerns surfaced by advisors are condensed into the row budget. Where a single risk has multiple findings as evidence, those are listed in the **Surface(s) / evidence** column. D5 is **the consolidation point**, not a re-derivation.

> **Done-bar (verbatim from plan):** *"each G-A..G-K gap has a row, and no unmitigated H/H row lacks a named owner."*

> **L × I semantics — no false precision.** H/M/L only. No numeric scoring. **Likelihood** = how likely is the risk to materialize in the next 12 months given current state. **Impact** = severity if it materializes (regulatory fine, FTC §5 enforcement, tenant churn, reputational, breach-notification). **L × I = "L/I"** in the table cell.

> **Owner semantics.** "Chris" = Chris Miller (Founder), the program owner. "Counsel-pending-on-trigger" = the answer comes from the Step 8 counsel package (PR #153), but **engagement is held until a real-party trigger fires** (see "Counsel triggers" below). "Path B" = the scheduling-session-owned now-items track (not in this program's direct ownership). "Closed" rows have already been remediated and are kept as audit trace, not active risks.

> **Counsel-engagement trigger list (program-wide).** The counsel input package (PR #153) is a **prepared artifact, held until a real-party trigger fires**. Triggers — any one:
> 1. Tenant signs an LOI / requests a DPA / demands a privacy attestation in vendor diligence (e.g., Atlanta tenant-#2).
> 2. A DSAR (consumer rights request) arrives requiring a substantive response.
> 3. A regulator inquiry arrives in any form (subpoena, CID, informal letter, attorney-general request).
> 4. A new product feature crosses a sensitivity threshold (explicit health-data handling; eligibility decisioning; payment instrument storage; minor-targeted flows).
> 5. A material commercial event (large foundation, healthcare-adjacent partner, enterprise customer) demands legal sign-off as part of onboarding.
>
> Engaging counsel today, absent these triggers, would be process theater. Counsel hours are bounded; engaging on hypotheticals produces memos that may shift before any trigger actually fires. The proportionate posture: package ready, triggers named, engage on first event.

> **Capability-now posture (the project delivers operational capability for what we collect today, regardless of trigger state).** Per the `compliance-implementation-advisor` fulfillment workflow review, the platform can respond to a DSAR today via the operator workflow below; what's missing is the artifacts that turn capability into ready-to-execute procedure. **Step 10 re-baseline names the DSAR tool + SLA alarm + Gmail config as the single remaining engineering deliverable.** Day-1 docs artifacts land in this PR alongside D5. See "Operational fulfillment workflow" below.

---

## Risk register

| # | Risk | Surface(s) / evidence | Tier (D4) | L × I | Current mitigation | Gap | Owner | Target action |
|---|---|---|---|---|---|---|---|---|
| **G-A** | **Live false widget claim** ("✅ No personal information stored permanently") + form-submissions persist forever (no `ttl` on `_store_submission`) → FTC §5 deceptive-practice exposure. | `Picasso/src/components/chat/StateManagementPanel.jsx:535` (claim); `picasso-form-submissions-staging` + `form_handler._store_submission` (persistence). Step 5 verification 2026-05-20 ([memory](../../../)). | 3 | **H / H** | None — both halves verified open in prod 2026-05-20. | UI claim is verifiably false today. | **Path B** (scheduling-session track) | (1) Path B ships widget-claim correction to chat.myrecruiter.ai; (2) Path B adds `ttl` write to `_store_submission` on lambda main. Verification re-run after Path B claims completion; row stays open until BOTH confirmed live in prod by independent re-check. |
| **G-B** | No living data-inventory process → surfaces accumulate without governance (Meta surfaces post-dated the original charter, etc.). | All §B/§C/§D surfaces. | n/a | M / H | **CLOSED** — CLAUDE.md PR rule from PR #148 (any PR adding DDB/Lambda/S3 PII surface must update inventory + tier in D4); D2 snapshot in PR #150. | None — closed by Phase 0.5 deliverables. | Closed | Audit-trace; no further action. Re-open if the PR rule is violated in practice. |
| **G-C** | Backup / PITR / S3-versioning / archive-bucket residue persists deleted data beyond delete-pipeline reach → "reasonable steps" question for counsel; auditor's immediate question under GDPR Art 17. | DDB PITR 35-day on every PII table (D2 Finding 10); S3 versioning on `myrecruiter-picasso{,-staging}` + `picasso-widget`; `ARCHIVE_BUCKET` archive of OLD_IMAGE session-summaries (D3 Finding 14). | 3 (across surfaces) | M / M | Apply-1 scaffold; no remediation. | No documented "reasonable steps" position; archive-bucket reachability undetermined. | **Counsel-pending-on-trigger** (S5 supplementary in PR #153) + **Chris** (operational verification post-counsel) | Counsel response on S5 → documented position + Step 10 roadmap update. Verify `ARCHIVE_BUCKET` name/region/encryption/lifecycle (D3 open item). |
| **G-D** | DSAR response-deadline data field exists with no enforcer — no SLA alarm, no escalation, no named backup handler → CCPA 45-day / GDPR Art 12(3) 1-month miss risk. | Path A historical design (§response handling); platform has not yet received a DSAR. | n/a | H / M | None today (no DSAR volume). | No SLA mechanism even at zero-volume. | **Chris** | One CloudWatch alarm or daily cron: any `pending` DSAR within 5 days of `intake+30d` emails owner; runbook names a backup handler. Small. Land alongside Apply-2 of Path A or sooner. |
| **G-E** | Counsel engagement gate has no owner / date / input package → tenant-#2 gate can never be satisfied (counsel lead time 3–4 weeks). | Original Path A roadmap. | n/a | n/a | **CLOSED** — Step 8 input package merged (PR #153), owner=Chris, send date will be recorded at engagement initiation. | None — closed by package merge. | Closed | Audit-trace; reopen if package is never sent. |
| **G-F** | DSAR access / portability (read side) under-designed vs delete — 2 sentences vs a 490-line delete design; same 45-day deadline. | Path A historical design. | n/a | M / M | None — Phase 4 of Path A is unbuilt. | Export coverage definition + surface list mirroring delete allow-list is missing. | **Chris** | 2-paragraph Phase-4 "export coverage" doc; mirror the delete allow-list. NOT new machinery — written definition only. Land when re-baselined Path A roadmap (Step 10) names Phase 4. |
| **G-G** | "Under-match approach is compliant" is an engineering risk-acceptance with no counsel cover; pushes completeness burden onto the data subject (alternate-identifier self-declaration). | Path A delete-walk design; D2 Finding 12 (Meta-PSID gap) makes this worse for Meta-only subjects. | 3 (data); 4 (access privilege) | H / M | None — engineering assertion only. | No counsel sign-off on "reasonable steps." | **Counsel-pending-on-trigger** (Q3 primary in PR #153) | Counsel response on Q3 → record determination in memo + Step 10 roadmap update. If counsel rejects under-match, redesign Phase 2 identity walk. |
| **G-H** | `picasso-employee-registry-v2-staging` (tenant-operator PII) scoped out of Path A with no documented legal basis. CPRA (2023) removed the employment exemption; CA employees now have full consumer rights. | `picasso-employee-registry-v2-staging` (D2 §B; D4 Tier 2). | 2 | H / M | None — `NOT-CONSUMER` scope flag in D2 is an internal classification, not a legal determination. | No counsel determination of rights-allocation between MyRecruiter and tenant employer. | **Counsel-pending-on-trigger** (Q2 primary in PR #153) | Counsel response on Q2 → determine if MyR has rights-fulfillment obligations; if yes, expand Path A scope. |
| **G-I** | No privacy-notice / point-of-collection disclosure determination. Controller/processor posture (tenant=controller; MyR=processor) is **assumed, not legally determined**. Affects every consumer-facing surface. | All inbound widget POSTs (D3 §F); tenant-config disclosure obligations (D3 §3). | 3 | **H / H** | None — silent assumption. | No CCPA/CPRA controller-processor determination; no compliant point-of-collection notice. | **Counsel-pending-on-trigger** (Q1 primary in PR #153) | Counsel response on Q1 → categories-of-obligations list → draft privacy notice + DPA + tenant disclosure in separate workstream. |
| **G-J** | `picasso-audit-staging` holds identifiers, retained for audit integrity, untouched by Path A delete → needs documented GDPR Art 17(3)(b) ("legal claims") basis, not just an "audit integrity" category label. | `picasso-audit-staging` (D2 §B / §H carve-out). | 2 | M / M | TTL-aged-out via `retention_expires_at` (writer-set). | No written Art 17(3)(b) basis. | **Chris** + **Counsel-pending-on-trigger** (advisory) | 2 paragraphs in the re-baselined Path A roadmap (Step 10) citing Art 17(3)(b) and the audit-integrity exception — land in Step 10. |
| **G-K** | Analytics surfaces "may carry PII attrs" — D2 Finding 3 + D4 rule 3a (sensitive-topic labels in intent signals). Classification is presumptive TTL-ONLY pending a spot-audit that has not run. | `picasso-session-events-staging` (D2 §B); `picasso-analytics-events-staging` S3 (D2 §C); intent signals (`donate_now`, `volunteer_application`, `chat_topic_classified: housing_assistance`, etc.). | 2 floor; 3 if rule 3a fires | M / M | TTL on the DDB rows; CMK on the S3 bucket. | Spot-audit unrun. If sensitive-topic labels are joined to session_id, rule 3a fires → Tier 3 + delete-scope expansion. | **Chris** | 10-file grep spot-audit of event payloads pre-Apply-2 (gate). If matchable identity attrs OR sensitive-topic labels present → reclassify to Tier 3 + add to delete walk. |
| **F9** | **Tenant-configured downstream destinations** (n8n / Sheets / CRMs / webhooks + per-tenant conditional S3 fulfillment buckets). Full Tier 3 form PII leaves the platform to destinations the **tenant (as controller)** has chosen — outside MyRecruiter's sub-processor scope. **Donor-class submissions (D4 rule 8 confidentiality) are a distinct mitigation framing.** | D2 Finding 9 + D2 Finding 8 (per-tenant fulfillment); D3 §G; D4 rule 8. | 3 (both content + confidentiality origins) | **H / H** | Partial: `tenant-sink-deletion-request.md` template (PR #154) coordinates DSAR with tenant. | Three load-bearing parts not yet complete: (a) MyR-published sub-processor list; (b) tenant DPA language requiring tenant DSAR-honor at downstream destinations; (c) counsel determination on Q1 (processor/controller posture). | **Chris** (a + c structurally) + **Counsel-pending-on-trigger** (Q1 in PR #153 + donor-disclosure S5) | **Three-part mitigation per Step 10 v3 re-baseline** — see `CONSUMER_PII_REMEDIATION.md` v3 §"Per-D5-row closure" F9 row. (NOTE: prior framing as "vendor inventory" conflated MyR-subprocessors-of-MyR with tenant-controlled-downstream-of-tenant; corrected 2026-05-20 in Step 10 re-baseline.) |
| **F11** | Apply-1 CMK (`kms-pii-staging`) exists but is applied to **no Tier 3 table**. All Tier 3 DDB rows remain SSE-DDB (AWS-owned key). Strategy doc Tier 3 control "restrict access" + Tier 4 "secrets manager" not yet met by encryption posture. | All Tier 3 DDB tables (D4 §B); D2 Finding 11; Apply-1 audit (memory). | 3 / 4 | M / H | Apply-1 scaffold (CMK + 3 IAM roles); CMK on no table yet. | Apply-2 deferred indefinitely pending Phase 0.5 outcome. | **Chris** (Apply-2 design gate) | Apply-2 design gate weighs CMK scope using D4 tier-map. Apply-2 preconditions G-3..G-7 must be satisfied before apply. Re-baselined Path A roadmap (Step 10) names Apply-2 disposition. |
| **F12** | **Meta-PSID → `pii_subject_id` mapping not represented.** Phase-1 lookup index keys on `normalized_email` only; Meta-only subjects (chatted via Messenger; never submitted email-bearing form) are currently **unreachable** by the Phase-1 identity walk. | `picasso-pii-subject-index-staging` PK schema; PSID-keyed rows in `staging-recent-messages`, `staging-conversation-summaries`, `picasso-channel-mappings-staging` (D2 §B; D3 Finding 12). | 3 (data) | H / M | None — gap discovered at D2/D3. | No PSID→subject-id linking design exists. | **Chris** | Design input for the re-baselined Path A roadmap (Step 10) + counsel Q3 (G-G) bears on this directly. If Q3 accepts under-match, a PSID-aware identity-graph extension lands as a Phase 2 add-on; if Q3 rejects, full redesign. |
| **F13 + F15** | **Bedrock-output surfaces:** (a) prompt persistence if account-level model-invocation logging is enabled (Finding 13, unverified); (b) AI response content-quality risk distinct from prompt persistence — hallucination / prompt-injection / KB-verbatim leak / solicitation/tax/eligibility claim risk (Finding 15); (c) KB content uploaded by tenants may embed PII and leak adjacent (Finding 16). | D2/D3 Findings 13, 15, 16; D4 §E. | 3 | M / H | (a) verified unset in IaC, account-level state unverified; (b) no content-quality controls beyond prompt design; (c) tenant KB hygiene is tenant-controlled. | Account-level Bedrock logging unverified; no content-quality SLA or hallucination-detection; KB hygiene contract absent. | **Chris** + **Counsel-pending-on-trigger** (S1, S2, S4 supplementary in PR #153) | (1) Verify `aws bedrock get-model-invocation-logging-configuration` per region in both accounts; (2) document AI-response-quality risk row owner; (3) KB-hygiene tenant contract = second-wave deliverable. |
| **F-DSAR1** | **Pre-Phase-1 form-submission rows lack `pii_subject_id`** → DSAR walker (form-submissions) misses any submission written before lambda #130 merged 2026-05-18. Subject deletion appears successful but a pre-Phase-1 row class remains, in tension with the Art 17 erasure obligation. **Scope:** form-submissions ONLY. notification-sends + notification-events match on `recipient == normalized_email` (not on `pii_subject_id`) and are NOT affected by the Phase-1 attribute gap; the analogous writer-normalization gap for those surfaces is tracked separately under F-DSAR3. | `picasso-form-submissions-staging` (D2 §B; D4 Tier 3); `Lambdas/lambda/picasso_pii_dsar_staging/lambda_function.py:_walk_form_submissions` coverage gap; theoretical-to-operational graduation triggered by DSAR Lambda landing 2026-05-20. | 3 | M / M | DSAR Lambda emits an explicit `manual_followup` on every invocation with a copy-pasteable `aws dynamodb scan --filter-expression 'submitter_email = :e ...'` CLI snippet substituting the operator's email + tenant_id (audit fix-now #3, 2026-05-20). Subject-not-found path emits the same snippet so pre-Phase-1 subjects get an actionable fallback even when the index returns null. | Procedural mitigation only — pre-Phase-1 rows are not reached by the automatic walker. Operator must execute the manual Scan from the followup. Durable fix = Apply-2 backfill of `pii_subject_id` onto pre-Phase-1 rows (deferred per Decision A / Phase 2 Apply-1 audit). | **Chris** | Operational: confirm Apply-2 backfill is named as a precondition for "all Phase-1-and-later rows reachable via Lambda alone" in the re-baselined Step 10 (item 1a / capability bundle). Until Apply-2 ships, the manual fallback in the DSAR Operator Playbook (deliverable item 7) is the standing procedure. **Severity remains H/H by impact when a pre-Phase-1 subject DSAR arrives**; M/M reflects expected DSAR frequency at current volume. |
| **F-DSAR3** | **notification-sends + notification-events `recipient` writer-normalization gap** — Writers (`Lambdas/lambda/Master_Function_Staging/form_handler.py:802` for email; `Lambdas/lambda/SMS_Sender/index.mjs:127` for SMS) store `recipient` verbatim without `.strip().lower()`. The DSAR walker would silently false-negative on a consumer DSAR for `Person@Example.COM` if the writer stored that mixed-case form and the operator's subject-identifier normalized to `person@example.com`. | `picasso-notification-sends-staging`, `picasso-notification-events-staging` (D2 §B; D4 Tier 3); flagged by 2-advisor audit of lambda PR #135 (Security-Reviewer + pii-data-lifecycle-advisor, 2026-05-21) — both advisors named it as the top FIX-NOW finding. | 3 | M / M | **DSAR walker (`_walk_notification_sends`) now does tenant-Query + case-insensitive Python post-filter** (`.strip().lower()` on both sides) — lambda PR #136 "audit fix-now-2" (2026-05-21). Walker bridges the gap with no writer-side change; new rows continue to be written un-normalized but are still reachable by the DSAR walker. | Walker is a procedural mitigation; durable fix is writer-side `.toLowerCase().trim()` normalization in `form_handler.py` and `SMS_Sender/index.mjs`. Writer change touches other Lambdas (different blast radius) so was deferred from the fix-now bundle. | **Chris** | Durable fix: writer-side normalization in MFS email + SMS paths. Defer until: (a) a Phase 1.5 / similar MFS normalization sweep is scoped; (b) operator workflow surfaces a real miss the walker fallback didn't catch; (c) tenant-count growth makes the walker post-filter RCU/bandwidth cost material. Re-classify L/I to L/L if writer normalization lands. |
| **F-DSAR2** | **DSAR Lambda IAM `DeleteItem` not bounded to operator-requested `tenant_id` via `dynamodb:LeadingKeys`** — tenant isolation is enforced in walker code only. A walker bug or compromised request payload could in principle trigger cross-tenant deletion within the surfaces the role can reach. | `infra/modules/lambda-pii-dsar-staging/main.tf` §`FormSubmissionsReadDelete` + walker `KeyConditionExpression` uses request `tenant_id` as PK; audit fix-now item #1 (Security advisor F-5) flagged the residual gap 2026-05-20. | 3 | L / H | Code-only tenant isolation: walker's `KeyConditionExpression = Key("tenant_id").eq(tenant_id)` bounds Query to one partition; `DeleteItem` Key includes the row's PK (recovered from the bounded Query). 4 dispatcher tests + 7 walker tests assert tenant boundaries hold (`Lambdas/lambda/picasso_pii_dsar_staging/test_dsar.py`). Operator workflow places a human in the loop on every invocation (`dry_run=true` default). | No IAM-level guard. **Dynamic per-request tenant_id is not expressible as a static IAM condition** — static `dynamodb:LeadingKeys` enumeration of all tenant_ids defeats isolation; per-tenant assumed-role pattern adds an STS hop + per-tenant role provisioning (over-engineered at <50 tenants); session-policy at invoke-time requires operator to construct policy per call (operationally fragile, error-prone). | **Chris** | **Documented as accepted residual risk under L/H Phase 0.5 posture.** Revisit triggers (any one fires): (a) tenant count crosses 50; (b) cross-tenant near-miss observed in integration tests (item 6); (c) operator role expands beyond Chris (multi-operator deployment); (d) any post-incident finding implicating cross-tenant blast radius. On trigger fire, design assumed-role pattern + ABAC migration. |
| **F-DSAR-C2-SSE-DEFER** (added 2026-05-22, fix-now-4 PR1 partial-apply) | **C2 SSE-KMS on `picasso-pii-dsar-audit-staging` deferred** — fix-now-4 PR1 attempted to associate the audit table with `kms-pii-staging` CMK; UpdateTable rejected with `AccessDeniedException: DynamoDB service principal does not have access to the key`. Root cause: CMK key policy (designed for Apply-1's delete/backfill/breakglass roles) lacks a `Principal: Service = dynamodb.amazonaws.com` Allow. Apply-2 precondition collision per Apply-1 phase-completion-audit (2026-05-19, "Apply-2 MUST NOT proceed until G-3/4/5/6/7 satisfied"). | `picasso-pii-dsar-audit-staging` (still default DDB SSE / AWS-owned key); `kms-pii-staging` key policy at `infra/main.tf:859-948`; fix-now-4 PR1 deferral decision 2026-05-22. | 3 | L / M | Audit table contents are DSAR metadata (dsar_id, event_timestamp, event_type, status, created_at_partition) + operator-side PII in `details` blob (operator email, tenant_id, error codes, walker outcomes) — **NOT consumer PII**. Default DDB SSE remains active. C2 DeleteItem-Deny resource policy DID ship (PR1) — immutability posture is intact. H3 CMK Allow + Deny-exception list DID ship (PR1) — forward-prep for when SSE-KMS lands. | C2 SSE-KMS not in effect; audit table at AWS-owned-key encryption-at-rest rather than scoped PII CMK. | **Chris** | Resolution paths (any one): (1) Apply-2 precondition PR adds `dynamodb.amazonaws.com` service-principal Allow + revisits G-3/4/5/6/7 → audit-table CMK association attempt resumes; (2) Tranche-2 commitment trigger fires (Atlanta LOI / first Meta DSAR / 90d post-tranche-1) → Apply-2 holistically addressed; (3) Counsel + operator determine default DDB SSE acceptable for operator-side audit metadata → C2 SSE-KMS waived with rationale. **Methodological note:** fix-now-4 plan v2.5's 6-reviewer convergence missed this collision; Apply-1 audit memory pointer was load-bearing context that should have been re-read before scope-finalizing C2. |
| **F-DSAR4** | **`staging-recent-messages` has no subject-linking attribute on row** — DSAR walker (recent-messages) reaches only sessions linked via form-submissions (chained walk). Subjects who chatted in the widget WITHOUT submitting a form have no durable subject linkage on the message row; the writer (`Lambdas/lambda/Master_Function_Staging/conversation_handler.py:762-770`) emits `sessionId/messageTimestamp/role/content/messageId/expires_at` only — no `email`, no `pii_subject_id`, no `tenantId`. Free-text `content` may carry user-typed PII (including third-party references). Structural gap distinct from F-DSAR1 (temporal): Apply-2 backfill cannot close F-DSAR4 because there's no subject identifier on the row to backfill against. | `staging-recent-messages` (D2 §B; D4 Tier 3 by virtue of free-text PII risk); `Lambdas/lambda/picasso_pii_dsar_staging/lambda_function.py:_walk_recent_messages` coverage gap; flagged by pii-data-lifecycle-advisor pre-implementation review 2026-05-21 as structural gap requiring distinct row from F-DSAR1. | 3 | M / M | **0-72h structural exposure window** = nominal 24h TTL (`MESSAGES_TTL_HOURS = 24` at `conversation_handler.py:44`) plus up to 48h DynamoDB TTL best-effort eviction grace (DDB guarantee is "within 48h of expiration"). A chat-only-no-form subject who DSARs within this 0-72h window since their last message is structurally unreachable by the walker. Walker emits explicit `manual_followup` documenting the gap, the TTL compensating control, F-DSAR1 inheritance cross-reference, and an operator-actionable CLI snippet for direct sessionId queries (when known out-of-band) + last-resort content-substring scan (flagged case-sensitive / false-positive-prone). Access-export projection drops internal identifiers (`sessionId`, `messageId`, `expires_at`) per Article 15 data-minimization. | TTL is a compensating control, NOT a primary one — a DSAR within the 0-72h window for a chat-only subject would miss them. Durable fix = MFS writer change (emit `pii_subject_id` or `subject_email_hash` on message row when subject is known from session context) OR rotate `sessionId` on each new subject-context boundary. Writer change has cross-Lambda blast radius (touches `Master_Function_Staging` request path) so deferred from the walker bundle. | **Chris** | Durable fix: writer-side subject-linkage emission in `Master_Function_Staging/conversation_handler.py`. Defer until: (a) a Phase 1.5 MFS-write-path sweep is scoped (would batch with F-DSAR3 writer-side normalization); (b) operator workflow surfaces a real chat-only-no-form DSAR miss; (c) DSAR volume + 0-72h-window timing makes the structural gap material. Severity holds M/M today (low DSAR frequency × narrow 0-72h structural-exposure window); H/H by impact when a chat-only-within-72h subject DSAR arrives. |
| **F-DSAR16** (added 2026-05-23, M1 phase-completion-audit row 25) | **12-month counsel-engagement safety floor** — Phase 0.5 closeout exit criterion #8 mandated explicit re-confirmation at 12-month mark from foundation close (2026-05-20 → **2027-05-20**) if no counsel trigger fires. Without a calendared backstop, indefinite deferral on counsel-pending rows (G-G, G-H, G-I, G-C, G-J, F9-b, F13-b) is the structural risk. M1 phase-completion-audit tech-lead N2 surfaced that the backstop was named in the master plan but NOT in the D5 register; this row corrects that. | All counsel-pending rows; Phase 0.5 closeout exit criterion #8 reference. | n/a | M / H if breached | Counsel package held under named-trigger discipline (5 triggers per `counsel-input-package.md`). Master plan §4 documents annual re-confirmation. | D5 register lacked a row to track the 12-month re-confirmation status; calendar event has not yet been created. | **Chris** | (1) Add calendar entry at 2027-05-20 to re-confirm counsel posture; (2) at re-confirmation, EITHER record "no trigger fired; continue holding" in [`dsar-log.md`](./dsar-log.md) OR proactively engage counsel if regulatory landscape has materially shifted. Row stays open until re-confirmation date (2027-05-20) passes with documented outcome. |
| **F-DSAR17** (added 2026-05-23, M3 archive-reachability verification) | **`picasso-archive-staging` versioning is ENABLED** — DSAR delete on the archive bucket leaves prior versions reachable for up to 7 days (per `NoncurrentVersionExpiration.NoncurrentDays=7` lifecycle rule). DSAR walker logic (M2 future scope: ARCHIVE_BUCKET walker) must enumerate versions via `aws s3api list-object-versions` and delete each with `--version-id` to fully erase. Default S3 delete (without `--version-id`) leaves a delete-marker but old versions persist for the 7-day window. | `picasso-archive-staging` S3 bucket (acct 525); see [`archive-reachability-decision.md`](./archive-reachability-decision.md) "Decision applied" section. | 3 (data, when populated) | L / M (bucket currently empty — preventive finding; severity will rise to M/M once `picasso-session-archiver` begins writing aged-out session-summaries at scale) | (a) Lifecycle rule `DeleteAfterOneYear` `NoncurrentVersionExpiration.NoncurrentDays=7` caps the leak window at 7 days. (b) Bucket is empty as of 2026-05-23 — no DSAR-impacting historical content. (c) M3 operator playbook (done-bar #6) carries the version-enumeration delete procedure for any DSAR before M2 ships the walker. | No automated walker for ARCHIVE_BUCKET versions yet; manual operator procedure in playbook is the standing mitigation. Long-term remediation (turn versioning OFF — one-shot, no data loss given empty bucket) requires either a new Terraform module for the bucket or a deliberate hand-managed operational change (current bucket appears hand-managed; absent from `infra/modules/`). | **Chris** | **UPDATED 2026-05-23 per advisor review (compliance 🟡-2):** preferred near-term action = **one-shot `aws s3api put-bucket-versioning --versioning-configuration Status=Suspended` against `picasso-archive-staging` NOW** (bucket is empty; zero data loss; removes the F-DSAR17 risk entirely until M9 audit). Drift created by CLI-not-IaC is acceptable trade-off given (a) bucket appears hand-managed already (absent from `infra/modules/`), (b) M9 TTL hygiene audit will codify either way. Watch trigger: **when `picasso-session-archiver` first writes a non-empty object, F-DSAR17 promotes to M/M and force-routes to M2 done-bar #1** (ARCHIVE walker must use `list-object-versions` enumeration). Original M9 options preserved as fallback: (1) add Terraform module for archive bucket + set `versioning_configuration.status = "Suspended"`; (3) keep ENABLED with M2 walker version enumeration as primary control. Row stays open until either bucket-versioning suspended OR M9 records a per-row TTL/versioning decision. |
| ✅ **F-DSAR19 CLOSED 2026-05-24** (originally added 2026-05-23 per advisor review — lifecycle 🔴 #5) | **Historical prod `picasso_form_submissions` rows persisted indefinitely.** Prod TTL was DISABLED pre-2026-05-23T18:01Z; enable was forward-only. **Closure 2026-05-24** via M4.G2 ship: 4 sprints (audit → spec + tech-lead-reviewer adversarial review → user-approved execution → closure docs); 46/46 UpdateItems succeeded with `ttl = submitted_at + 365d` (same formula as active BSH writer); §7 post-condition scan returned `Count=0`. Audit population was small (47 rows total / 46 backfill candidates; all `<180d` old) → spec collapsed from 3-tier to 1-tier (strictly more conservative than original for this dataset). | `picasso_form_submissions` (prod-614; D2 §B; D4 Tier 3); all 46 historical rows now have `ttl`. Closure refs: picasso PR #188 (decision doc + execution log) + `m4g2-prod-ttl-backfill-execution-log-2026-05-24.md` (operator-attested audit artifact since prod CloudTrail does NOT capture DDB data events — see F-DSAR21 for the broader observability gap). | 3 (data) | L / L (closure complete; evictions scheduled 2027-01-03 to 2027-05-05 inclusive) | ✅ Closed. All 46 historical + future BSH-written rows now share the same `submitted_at + 365d` retention envelope. Corrected widget claim is now defensible end-to-end. | ✅ Closed. Residual concern (per-`form_type` TTL matrix — e.g., do contact forms need shorter retention than volunteer apps?) is a SEPARATE gap tracked under F-DSAR23 / M4.G3. | **Chris** | ✅ **CLOSED** via M4.G2 ship. Execution log committed at `docs/roadmap/PII-Project/m4g2-prod-ttl-backfill-execution-log-2026-05-24.md`. The mystery row `volunteer_dare2dream_1776194362503` (pre-existing `ttl=1807730427` ≈ `submitted_at+364d`; informational; idempotency skipped) is recorded in the execution log but source remains unidentified — does not affect closure. |
| ✅ **F-DSAR20 CLOSED 2026-05-24 (re-affirmed post-Sprint-G audit-of-audit 2026-05-24)** (originally added 2026-05-23 per advisor review — lifecycle 🔴 #10 + compliance 🟡-3; phase-completion-audit 2026-05-23 added security 🔴 sub-item for dry-run + wrapper; **post-closure phase-completion-audit 2026-05-24** spawned 3 adversarial reviewers — 24 findings surfaced [6 🔴 + 9 🟡 + 4 🟢 + 5 ℹ️]; user dispositioned all "fix everything"; closed via **Sprint G1-G4** [picasso PR #212 wrapper hardening, picasso PR #213 playbook surgical fixes, Sprint G4 operator-attested rehearsal executed 2026-05-25T00:33:01Z with UUID-keyed synthetic identifier returning `Count=0` across all 4 surfaces — picasso_form_submissions:47 / picasso-notification-sends:33 / production-recent-messages:2 / picasso-session-summaries:863, and Sprint G3 closure docs in this PR]; "rehearsal not operator-executed" residual from original closure is now empirically resolved) | **Operator playbook §3-§6 CLI snippets referenced staging tables + staging profile only; prod-CLI variant not documented or tested.** Original advisor framing: "foot-cannon, not foot-gun" (lifecycle); "first prod DSAR will improvise — exactly the moment a regulator most wants to see rehearsed procedure" (compliance). **Closure 2026-05-24** via M9.G4 ship: 3 sprints across picasso PR #207 (Sprint A docs) + picasso PR #208 (Sprint B wrapper) + this PR (Sprint C closure). **Sprint A** added §3.1/§4.1/§6.1 "Prod DSAR fulfillment" sections to playbook with: (a) `sts get-caller-identity` account guard refusing if not on 614; (b) pre-substituted prod CLI snippets (operator should not mentally substitute under SLA pressure per advisor); (c) manual-walk surface checklist with operator initials per surface; (d) dry-run rehearsal procedure with synthetic identifier `rehearsal-noop@example.invalid`. §3.1's single-source-of-truth staging↔prod substitution table empirically verified via `aws dynamodb list-tables --profile myrecruiter-prod` 2026-05-24 (documents 6 surfaces that do NOT exist in prod — PII subject-index, DSAR audit table, channel-mappings, archive bucket — with operational implications spelled out for each). §4.1 adds delete-specific safety: §3.1-access-first prerequisite; describe-table pre-check; notification-sends carve-out. Existing §7 PROD-CLI WARNING flipped to ✅ closure indicator with section links. Playbook header v1.0 → v1.1. **Sprint B** added `tools/dsar-invoke.sh` (closes phase-audit security 🔴 sub-item): account guard via sts get-caller-identity (source of truth, not profile name); dry-run default with force-override on operator payload; explicit prod write confirmation (banner + typed "DELETE PROD" verbatim). Live smoke-tested against staging Lambda — all 3 safeties verified end-to-end. Wrapper is forward-looking — for when a prod-twin DSAR Lambda eventually ships (none today; Lambda is staging-only). **Sprint C** (this row): D5 + master plan v0.20 closure docs. | `dsar-operator-playbook.md` §3.1/§4.1/§6.1 + updated §7 WARNING block; `tools/dsar-invoke.sh`. Closure refs: picasso PR #207 (`e8e684b`) + picasso PR #208 (`339bc19`). | n/a (procedural) | L / L (closure complete; operator now has tested prod-CLI procedures + automated safety wrapper for any future prod-twin Lambda) | ✅ Closed. **New primary controls**: §3.1 prod section with account guard + pre-substituted snippets (operator-attested); shell wrapper enforces guard + dry-run for any Lambda invocation. **Residual**: actual first-prod-DSAR rehearsal (dry-run with synthetic identifier against real prod data) has not been operator-executed yet — playbook documents the procedure but the rehearsal itself awaits operator availability. Not a closure blocker since the §3.1 substitution table was empirically verified during M9.G4 work itself. | ✅ Closed. Anomaly flagged during Sprint B smoke test: staging DSAR Lambda returned a placeholder-shaped response body even though CodeSha256 matches the Sprint-F1-deployed shape — filed for separate investigation (possible IaC-vs-deployed-code drift, staging variant of F-DSAR21 prod-IaC-drift gap; routed to F-DSAR21/M9.G5 backlog). Wrapper's contract independently validated. | **Chris** | ✅ **CLOSED** via M9.G4 ship. Master plan v0.20 records closure + Sprint A/B/C narrative. Future maintenance: if §3/§4/§6 in playbook changes (new step, new flag), update §3.1/§4.1/§6.1 in the same PR — duplication is intentional per advisor (operator should not mentally substitute under SLA pressure) but creates sync debt. |
| ✅ **F-DSAR21 — Step 1 + ALL Step 1.5 sub-items CLOSED 2026-05-25** (new **F-DSAR29** spawned from Sprint B anomaly — DSAR Lambda artifact-vs-source disconnect — USER DISPOSITION REQUIRED) (originally added 2026-05-23 per advisor review — compliance 🔴-4; **scope-expanded 2026-05-24** post-M9.G4 audit-of-audit; **Step 1 closure 2026-05-25** via M9.G5 ship of `tools/pii-prod-drift-audit.sh` + `prod-iac-drift-audit-2026-05-25.md`; **Step 1.5 writer-pair closure** via `writer-pair-enumeration-2026-05-25.md` — closes item (c); 3 residual concerns recorded NOT-FIX-NOW; **Step 1.5 prod-CT closure** via `prod-ct-cw-logs-export-2026-05-25.md` — closes item (b); empirical correction to M9.G7 routing premise; defer-with-trigger; **Step 1.5 audit-table CT closure** via `audit-table-ct-data-events-2026-05-25.md` — closes item (d); (D) defer-with-trigger aligned with sibling F-DSAR-SoD; **Step 1.5 Sprint B anomaly closure** via `sprint-b-anomaly-investigation-2026-05-25.md` — closes item (a); deployed Lambda definitively confirmed as 7-line 305-byte placeholder stub vs 1600+ lines of source across 12 source commits; new F-DSAR29 row spawned for user disposition between (A) deploy real source / (B) re-frame closure narratives / (C) both / (D) defer-with-trigger) | **No control/audit for prod hand-managed IaC drift on PII surfaces.** Prod table `picasso_form_submissions` TTL=DISABLED was caught only by accident mid-M4-promotion 2026-05-23. The general class: "what else is misconfigured in prod that staging IaC assumes is correct?" had no answer until 2026-05-25. **Step 1 closure 2026-05-25**: read-only audit script shipped + executed against prod-614 covering 5 PII surfaces (form_submissions / notification-sends / notification-events / recent-messages / session-summaries) on 4 dimensions (TTL / PITR / SSE / DeletionProtection). **Findings**: uniform drift — PITR=disabled on all 5 surfaces (staging IaC expects enabled on the 4 PII-bearing tables); TTL/SSE/DeletionProtection all MATCH expected state. **Drift classified (c) accept-with-named-trigger** for all 5 surfaces per `prod-iac-drift-audit-2026-05-25.md` §"Drift disposition" — PITR is DR/availability not privacy/compliance; mutation cost non-trivial; no active blast radius; non-zero recurring AWS cost; uniformity signals structural cause (IaC convention added after prod tables existed). **Step 1.5+ sub-items remain DEFERRED-with-trigger under M9.G5** (each right-sized as own sub-PR per the audit-vs-remediation methodology lesson): (a) **Sprint B placeholder-body anomaly** — staging DSAR Lambda returned placeholder-shaped response despite CodeSha256 match; needs deployed-code SHA vs source SHA investigation; (b) **M9.G7 prod-CT-UpdateTimeToLive sub-item** — prod-614 CloudTrail has `CloudWatchLogsLogGroupArn=null` so no UpdateTimeToLive alarm possible; routed here per v0.18 retroactive ratification; (c) **Python+Node writer-pair enumeration** — list which Lambdas have which write path active in prod per surface; (d) **Audit-table CloudTrail data-events config** — compensating control for the audit-table tamper structural concern (operators need PutItem to write legitimate manual audit rows; tamper visibility would come from CT data-events). | All prod-614 PII tables audited 2026-05-25: `picasso_form_submissions`, `picasso-notification-sends`, `picasso-notification-events`, `production-recent-messages`, `picasso-session-summaries`. Discovery context: M4 prod promotion 2026-05-23 + M9.G4 audit-of-audit 2026-05-24 + M9.G5 Step 1 ship 2026-05-25. | n/a (process) | M / L (Step 1 closed reduces likelihood and clarifies impact — the drift dimension is now known and bounded; medium likelihood remains because Step 1.5 sub-items could surface additional drift classes; impact now LOW given (c) classification of the PITR drift) | Step 1 audit script (`tools/pii-prod-drift-audit.sh`) executes in ~30s against 5 surfaces; re-runnable quarterly (Step 2). Ad-hoc per-deploy verification (relies on operator catching the gap before damage). Lambda env-var inspection during deploys. | Step 1.5 sub-items not yet executed (Sprint B anomaly investigation; writer-pair enumeration; audit-table CT data-events; prod CT→CW Logs export). | **Chris** | **Step 1 CLOSED 2026-05-25** via M9.G5 ship — `tools/pii-prod-drift-audit.sh` + `docs/roadmap/PII-Project/prod-iac-drift-audit-2026-05-25.md`. **Step 1.5+ deferred-with-trigger** (each its own scoped sub-PR per `prod-iac-drift-audit-2026-05-25.md` §"Methodology lessons" #3). **Step 2 (ongoing):** quarterly re-run of `pii-prod-drift-audit.sh` tied to D2/D3/D4 currency review pair (next 2026-08-23). **Step 3 (deferred):** when prod cuts over to per-resource Terraform per P0 SOP, prioritize PII tables first (PITR drift would self-resolve at that point). **Activation triggers for Step 1.5 sub-items**: (a) Sprint B anomaly — any second occurrence of CodeSha256-vs-deployed-behavior drift; (b) prod-CT export — first need for prod UpdateTimeToLive alarming OR next prod CloudTrail config change; (c) writer-pair enumeration — first PII surface added with dual-writer concern OR M2 (Meta/S3/ARCHIVE walker extension); (d) audit-table CT data-events — first audit-table tamper concern OR Atlanta tenant LOI. |
| **F-DSAR-SoD** (added 2026-05-24 per M9.G4 audit-of-audit — Security S3 strong-rec, recorded for trigger) | **Separation-of-duties for prod DSAR deletion is currently single-actor.** `tools/dsar-invoke.sh` typed-confirm ("DELETE PROD" verbatim) is the only gate; no second-operator review required for irreversible prod DeleteItem on consumer PII. Recorded explicitly (rather than left implicit) so future-self knows this is a deliberate risk-acceptance, not an oversight. | `tools/dsar-invoke.sh` + `dsar-operator-playbook.md` §4.1 SoD posture note. Discovery: M9.G4 audit-of-audit 2026-05-24. | n/a (procedural) | L / M (low today — sole operator; M by impact — operator-error or compromised single SSO session = data-loss event affecting real consumers) | Single-actor typed-confirm via wrapper + manual playbook checklists. | No 4-eyes for prod DeleteItem; no second-operator review. | **Chris** | When triggered: add mandatory 4-eyes review for prod DeleteItem to `tools/dsar-invoke.sh` (second operator types confirm-counterpart) + playbook §4.1 (two-operator initials). Triggers (any one): (a) second operator joins ops rotation; (b) Atlanta LOI (M6); (c) regulator inquiry on operator-controls. No calendar backstop — gap is acceptable at current scale. |
| ✅ **F-DSAR22 CLOSED 2026-05-23** (originally added per advisor review — compliance 🔴-1 + 🔴-5) | **M3 done-bar #2 fault-test did not empirically prove the secondary control fires independently of operator initiation.** The test used SNS `FilterPolicy` (filters one subscription's delivery; does not exercise the original done-bar's "disable SNS topic" failure mode) and the operator manually ran the weekly Monday CLI scan in the same hour, knowing where to look. A real alarm miss happens when the operator is unaware. **Closure 2026-05-23** via M9.G6 ship: belt-and-suspenders weekly reminder Lambda (`picasso-pii-dsar-weekly-reminder-staging`) LIVE in acct 525; independent IAM role + EventBridge weekly schedule + embedded operator-runnable CLI snippets; test-fired end-to-end with SNS publish + delivery verified. Quarterly fault-test re-run cadence (replaces v0.7's annual) applies going forward. | `dsar-operator-playbook.md` §8 fault-test result; M3 done-bar #2; SLA monitor `picasso-pii-dsar-sla-monitor-staging`. Closure: lambda PR #146 + picasso PR #184 (`cdd039d`) merged 21:19Z 2026-05-23. | n/a (procedural) | L / M (residual L/L now that automated independent reminder ships every Monday) | **New** (closure): weekly reminder Lambda `picasso-pii-dsar-weekly-reminder-staging` (dedicated IAM, independent EventBridge schedule, shared SNS topic acceptable shared-failure). **Existing** (retained): weekly Monday CLI scan, Google Calendar reminder, EventBridge alarm + SNS delivery normal flow. | ✅ Closed. Quarterly fault-test re-run cadence pairs with D2/D3/D4 currency review. New monitoring surface: M9.G7 will add CW Errors alarm on the reminder Lambda itself. | **Chris** | ✅ **CLOSED** via M9.G6 ship. Original options: (1) Google Calendar (operator OAuth-pending); (2) **belt-and-suspenders cron — chosen + shipped**; (3) operator-blinded re-test (deferred). M3 done-bar #2 status restored to DONE (no residual PARTIAL caveat). |
| **F-DSAR23** (added 2026-05-23 per advisor review — compliance 🟡-1 + lifecycle 🟡-1) | **Surviving widget bullet "✅ Data retention: 30 minutes session storage" is misleading-by-omission post-M4 #2.** With the false claim "No personal information stored permanently" removed (M4 #1) and 365d server-side TTL now writing on form-submission rows (M4 #2), the remaining bullet implies "MyR retains my data for 30 minutes" — but that's browser-side sessionStorage / widget UI cache only. A consumer reading it would not infer the 365-day server-side retention or DSAR audit-table append-only carve-out. Substantively misleading by omission. Combined separate finding: 365d blanket TTL ignores data-minimization per `form_type` (volunteer signup vs donor vs contact form vs free-text comments — D4 rule 8 vulnerable-population framing pushes shorter for donor/volunteer). | `Picasso/src/components/chat/StateManagementPanel.jsx:535-538` (Privacy & Compliance section, surviving 3 bullets); `Lambdas/lambda/Bedrock_Streaming_Handler_Staging/form_handler.js:574` (365d ttl). Discovery context: advisor review 2026-05-23. | 4 (consumer-facing trust) | L / M (L because the surviving bullet is technically about browser storage; M because consumer-interpretation mismatch creates the kind of misleading-by-omission that fed G-A in the first place) | M4 #1 ships a less-misleading widget than pre-M4. Privacy notice (M8.G1) is the canonical disclosure surface; widget bullets are operator-facing settings panel detail. | Surviving bullet's scope (browser-side vs server-side) is not disclosed in widget. 365d TTL not differentiated by `form_type`. | **Chris** | Two parts: (a) **widget edit (~0.25d):** qualify the surviving bullet ("...of chat history in your browser; server-side retention varies by submission type — see privacy notice") OR remove until M8 privacy notice publishes. (b) **per-`form_type` TTL matrix (~1d):** rows in BSH `form_handler.js` derived from `formId` → TTL window (volunteer = 180d; donor = 90d; contact = 60d; default 365d). Triggers: (a) M8.G1 privacy notice drafting starts (would batch with widget edit); (b) counsel-Q1 (G-I) response if it imposes retention windows; (c) Atlanta LOI gate (tenant-trust posture); (d) **calendar backstop: 2026-06-22 (30 days from discovery — tightened per phase-completion-audit 2026-05-23, tech-lead 🔴 + security 🟡)**. |
| ✅ **F-DSAR24 CLOSED 2026-05-24** (originally added 2026-05-23 per phase-completion-audit — code-reviewer 🔴 HIGH) | **BSH `form_handler.js` try-catch silently swallowed DynamoDB `PutCommand` errors.** Catch block at lines 601-622 (post-fix) was previously a 3-line silent catch at 579-585. **Empirical 2026-05-14T13:39:29Z staging incident** (1 real form submission lost; `AccessDeniedException` due to env-var-table-name drift) confirmed this was no longer hypothetical when M9.G8 work started. **Closure 2026-05-24** via M9.G8 ship: scope decision = **option 3 variant** (keep the catch — preserves consumer UX intent; make the silent catch no longer silent at the alerting layer). lambda PR #156 enriches the error log line with structured `tenant_id` + `submission_id` + `error_name` + `error_message` (preserves "Error saving to DynamoDB:" prefix as the metric-filter contract). picasso PR #205 ships `aws_cloudwatch_log_metric_filter.form_handler_ddb_write_error` (namespace `Picasso/BSH/FormHandler`, metric `FormSubmissionDDBWriteError`, default_value=0) + alarm at ≥1 in any 5-min window → ops-alerts SNS. New jest test `should log structured tenant_id + submission_id when DDB PutCommand fails — M9.G8 / F-DSAR24 closure` regression-guards the wire-format contract. **Fail-closed (option 1) explicitly rejected** — would break consumer submissions on any IAM/config drift (worse failure mode than the silent catch). **DLQ+retry (option 2) explicitly rejected** — ~1d engineering overhead unjustified at 1/90d observed cadence. | `Bedrock_Streaming_Handler_Staging/form_handler.js:601-622` (catch block); `infra/modules/lambda-bedrock-handler-staging/main.tf` (metric filter + alarm). Closure refs: lambda PR #156 (`bc0c13e`) + picasso PR #205 (`3f0465a`). | 3 (data) | L / L (closure complete; next incident detected within 5 min via alarm; operator identifies lost submission from structured log fields) | ✅ Closed. **New primary control:** structured log line + CW Logs metric filter + alarm. Lambda continues to preserve consumer UX (returns success even on DDB failure) but silent-catch path is now operator-visible within 5 min. | ✅ Closed. Residual: actual lost submissions are not auto-recovered (consumer would need to resubmit) — that's the price of option 3 vs option 2's DLQ. Operator can identify which `submission_id` was lost from the structured log line. Future Atlanta LOI or scale-up might justify promoting to option 2 (DLQ+retry consumer); routed as deferred-with-trigger to M9 backlog. | **Chris** | ✅ **CLOSED** via M9.G8 ship — option 3 variant chosen + executed. 505/505 BSH tests pass (new F-DSAR24 test included). Master plan v0.19 records closure + the rejected-option rationale. |
| ✅ **F-DSAR25 CLOSED 2026-05-24** (originally added 2026-05-23 per phase-completion-audit — security-reviewer 🟡 UNASKED FINDING) | **Unescaped form_data values in internal notification email HTML body.** BSH `form_handler.js` default-path HTML body interpolated consumer-controlled `key` + `value` from `formData` directly into a staff-facing `<table>` without `escapeHtml()`. The helper exists at line 79 and IS applied to `otherRecipients` (line 1193) and tenant-controlled `orgName` (sendConfirmationEmail), but the default notification path missed the `<td>` cells. **Closure 2026-05-24** via M9.G8 ship: lambda PR #156 wraps both `key` and `value` with `escapeHtml(String(value ?? ''))` (helper handles `&`, `<`, `>`, `"`). Title-case transformation from `buildFormDataDisplay` upstream means keys are typically safe (filtered/normalized) but escape is applied as defense-in-depth. New jest test `should escape consumer-controlled form_data in staff notification HTML — M9.G8 / F-DSAR25 closure` verifies XSS payloads (`<img onerror>`, `<script>`, quoted content) are properly escaped in staff-facing HTML. | `Bedrock_Streaming_Handler_Staging/form_handler.js` default-path HTML body (post-fix at ~line 1180); `escapeHtml()` helper at line 79. Closure ref: lambda PR #156 (`bc0c13e`). | 4 (staff PII exposure via XSS in staff email client) | L / L (closure complete; values escaped end-to-end; key escape is defense-in-depth above upstream filter) | ✅ Closed. **New primary control:** `escapeHtml()` applied to both `key` and `value` at interpolation site; matches the surrounding `otherRecipients` + `orgName` escape pattern. Defense-in-depth retained: email client XSS filters (Gmail/Outlook) remain the standing secondary control. | ✅ Closed. Future maintenance note: if a new `htmlBody +=` interpolation is added in `sendInternalNotificationEmail`, ensure consumer-controlled fields are wrapped in `escapeHtml(String(value ?? ''))` matching this pattern. | **Chris** | ✅ **CLOSED** via M9.G8 ship (single-line fix matched the D5-row estimate of ~10 min for code + ~5 min for jest test). 505/505 BSH tests pass with the new F-DSAR25 jest test. |
| ✅ **F-DSAR26 CLOSED 2026-05-24** (originally added 2026-05-23 per phase-completion-audit) | **SLA monitor Lambda had no CW Errors alarm + no DLQ.** **Closure 2026-05-24** via M9.G7 ship (picasso PR #189 `395a4c4`): `aws_cloudwatch_metric_alarm.monitor_errors` (Errors >0 in 5min → ops-alerts SNS) + `aws_sqs_queue.monitor_dlq` (14d retention) + `aws_lambda_function_event_invoke_config.monitor_dlq_wire` (on_failure → DLQ; 2 retries; 6h max event age). Bonus: same alarm pattern on M9.G6 weekly reminder Lambda. | `infra/modules/lambda-pii-dsar-sla-monitor-staging/main.tf` + `lambda-pii-dsar-weekly-reminder-staging/main.tf`. Closure ref: picasso PR #189. | n/a (alarming gap) | L / L | ✅ Closed. CW Errors alarms on both Lambdas → ops-alerts SNS; SQS DLQ on SLA monitor. | ✅ Closed. | **Chris** | ✅ **CLOSED** via M9.G7 ship. |
| 🟡 **F-DSAR27 PARTIAL-CLOSED 2026-05-24** (originally added 2026-05-23 per phase-completion-audit; integration suite deferred with 90d calendar backstop 2026-08-22) | **SLA monitor Lambda test coverage gaps (5 items).** **Partial closure 2026-05-24** via M9.G7 Sprint A (lambda PR #149 `2183b94`): 4 of 5 items shipped as unit tests — (a) `test_ddb_table_called_with_correct_name` (env-var binding regression guard; tightened in Sprint E2 / audit D6 via `assert_any_call` + 1 candidate to exercise BOTH StatusIndex + per-candidate Table calls); (b) `test_handler_idempotent_on_eventbridge_replay` (replay-safety invariant + asserts no DDB side effects); (c) `test_lambda_timeout_mid_loop_does_not_publish_partial` (mid-loop ClientError aborts before SNS publish); (e) `test_event_timestamp_iso_format_contract` (pins writer/reader format alignment for DDB lexicographic comparison; Sprint E2 D8 adds companion writer-side `test_now_iso_format_contract` in test_dsar.py). Plus reader-side `timespec='microseconds'` tighten + writer-side `_now_iso` pinning comment. (d) integration test suite vs acct 525 (1-2d long pole) is **DEFERRED-with-trigger + 90d calendar backstop 2026-08-22**. | `picasso_pii_dsar_sla_monitor_staging/test_sla_monitor.py` (now 14 unit tests, +4 from M9.G7) + `picasso_pii_dsar_staging/lambda_function.py:_now_iso` writer-side comment + Sprint E2 D8 contract test. Closure ref: lambda PR #149 + lambda PR #152. | n/a (test coverage) | L / L (closure-of-units complete; integration deferred but unit-test mock surfaces are accurate per current Lambda contract) | ✅ Units closed. Integration deferred — triggered by new alarm fires, M9-class addition, OR 2026-08-22 calendar backstop. | Integration suite remains open for trigger; not currently a defensibility risk because the units cover the per-fault-mode contract. | **Chris** | ✅ 4 of 5 sub-items CLOSED via M9.G7 ship. Integration suite (5th sub-item) deferred-with-trigger. **Sprint E5 / audit D9 (2026-05-24): 90d calendar backstop 2026-08-22 added** — per tech-lead-reviewer 🟡 #4 finding that the original trigger language ("if alarm fires") was non-firing in practice (post-incident diagnosis rather than preventive). At backstop date: EITHER ship integration suite OR record "no trigger fired; alarm-correctness inferred from unit coverage + production absence-of-incidents; defer another 90d with rationale" in master plan revision history. |
| ✅ **F-DSAR28 CLOSED 2026-05-24** (originally added 2026-05-23 per phase-completion-audit) | **Audit-table smoke row hygiene unverified.** **Closure 2026-05-24** via M9.G7 Sprints B + C (picasso PR #189 + staging-only one-shot script). Sprint C: identified 47 unique `smoke-*` dsar_ids with open `request_received` events on `picasso-pii-dsar-audit-staging`; appended synthetic `closed` events for each (audit table is append-only per C2 4-action Deny — no DeleteItem option). Sample verification: `smoke-sla-monitor-001` now has 2 closed events; SLA monitor's `_has_closed_event` PK Query will skip it. Sprint B: playbook §8 JMESPath `!starts_with(dsar_id.S, 'smoke-')` filter suppresses future test artifacts from operator's manual scan. | `picasso-pii-dsar-audit-staging` (acct 525); `dsar-operator-playbook.md` §8. Closure refs: picasso PR #189 + Sprint C operational script (not in repo — one-shot). | n/a (operational hygiene) | L / L | ✅ Closed. Existing rows skipped by SLA monitor's `_has_closed_event` check; future smoke rows suppressed by playbook §8 filter. | ✅ Closed. (Note: a server-side smoke-row filter on the SLA monitor Lambda itself would be even more durable; not in M9.G7 scope but flagged as a potential M9-class future addition trigger.) | **Chris** | ✅ **CLOSED** via M9.G7 ship. |
| ✅ **F-DSAR29 CLOSED 2026-05-25** via Option C (deploy + re-frame) — `f-dsar29-disposition-2026-05-25.md` (originally added 2026-05-25 per M9.G5 Step 1.5 Sprint B anomaly investigation) | **DSAR Lambda artifact-vs-source disconnect — RESOLVED.** Original gap: Deployed `picasso-pii-dsar-staging` Lambda (CodeSha256 `2tQsnrZ9vA0V7DJQH+1RBPgAF0XDbKY/d31OXChgvtg=`) is a 7-line / 305-byte placeholder stub returning a plain-text body pointing operators to `aws lambda update-function-code from Lambdas/lambda/picasso_pii_dsar_staging/`. Real source has 1600+ lines with full DSAR walker logic. **The deployed Lambda has never run the actual code.** Empirically established 2026-05-25 via `aws lambda invoke` + `aws lambda get-function` + zip download + content diff. **Implication:** prior closure narratives across M1.G6 (lambda#141), Sprint E1 (lambda#150), Sprint E2 (lambda#152), Sprint F1 (lambda#153), Sprint F2 (lambda#154), M9.G4 Sprint B (`tools/dsar-invoke.sh` smoke-test) implicitly claimed "Lambda deployed" when the artifact in AWS is a stub. Unit + integration tests against source code paths are intact (they import the module directly; don't go through Lambda invoke); the deployed artifact does not run them. | `picasso-pii-dsar-staging` Lambda (acct 525); deployed zip 373 bytes vs source 1600+ lines. Discovery context: M9.G5 Step 1.5 Sprint B anomaly investigation. | n/a (deployment-artifact integrity; downstream privacy impact is none today because operator-manual fulfillment per playbook §3.1 doesn't traverse Lambda) | L / L (today — Lambda is non-load-bearing; operator-manual fulfillment is the standing procedure per playbook §3.1; no consumer DSAR has ever flowed through Lambda); H / M (when triggered — first prod DSAR or Atlanta LOI gate; operator under SLA pressure reaching for Lambda-mediated execution gets placeholder response) | Today: operator-manual `aws dynamodb scan` via playbook §3.1; wrapper `tools/dsar-invoke.sh` safeties intact regardless of Lambda body shape. | Lambda deploy artifact has never matched source; closure narratives need reconciliation (truthfulness debt). | **Chris** (USER DISPOSITION REQUIRED) | **Options (user disposition required):** (A) Deploy real source now — zip `Lambdas/lambda/picasso_pii_dsar_staging/` + `aws lambda update-function-code` + populate Environment.Variables (AUDIT_TABLE, FORM_SUBMISSIONS_TABLE, NOTIFICATION_SENDS_TABLE, NOTIFICATION_EVENTS_TABLE, RECENT_MESSAGES_TABLE, SESSION_SUMMARIES_TABLE, PII_SUBJECT_INDEX_TABLE) + verify staging IAM scope + smoke-test via wrapper. ~1-2h. **Most truthful.** (B) Re-frame closure narratives as "source-only ship; deployed artifact unchanged from placeholder by design pending M2 prod-twin gate." ~30min master plan revision-history clarification on each affected closure. **Most honest.** (C) Both (A) deploy + (B) re-frame. **Most complete.** (D) Defer-with-trigger: add playbook explicit note "Lambda is not deployed; do not rely on Lambda invocation for DSAR fulfillment"; defer (A) until first-prod-DSAR-ever OR Atlanta LOI. **Minimum operational change.** Calendar backstop 2026-08-22 D2/D3/D4 currency review regardless of option chosen. |
| 🆕 **F-DSAR30** (added 2026-05-25 per M2 Sprint A design — `m2-design-2026-05-25.md` §3.3) | **`identifier_type` ∈ {phone, name+address} not walker-supported in M2.** M2 Sprint A walker-matrix design (§4) documents `phone` and `name+address` as walker-not-supported — neither maps to a tenant-scoped subject identifier today. Phone numbers appear on `picasso-notification-sends-staging` (already covered by M1 walker via `pii_subject_id` resolution from email) and `picasso-sms-consent-staging` (opt-in artifact, not subject-rights-actionable as a primary identifier). Name/address are free-text within `picasso-form-submissions-staging.responses` map — no index/projection exists. Subjects without an email or PSID identifier require manual M3 playbook procedures. **Walker NOT implemented in M2** per master plan done-bar #5 directive ("implement walker support OR record explicit walker-not-supported + manual procedure in M3 playbook + named D5 row"). | DSAR Lambda `SUPPORTED_IDENTIFIER_TYPES = {"email", "psid"}` post-M2 Sprint B; `picasso-sms-consent-staging`, `picasso-form-submissions-staging.responses` (free-text). Discovery context: M2 Sprint A walker-matrix synthesis 2026-05-25. | n/a (capability scope decision) | L / L (subject populations small per current scale; manual scan + FilterExpression procedures documented; no operator urgency at solo-operator scale) | M3 playbook §"Per-surface manual fallback procedures" gets two new entries in Sprint E (one per unsupported identifier_type). Existing M1 email walker covers phone-on-notification-sends and name-on-form-submissions cases when the subject is also reachable by email. | Phone-only and name+address-only subjects (no email, no psid) cannot be served by Lambda; manual operator procedure is the standing path. Trigger to revisit: first prod DSAR request with `identifier_type ∈ {phone, name+address}`. | **Chris** | **M2 Sprint E ships**: (a) playbook §"Per-surface manual fallback" entries for phone + name+address (manual `aws dynamodb scan --filter-expression` snippets per surface); (b) Lambda `_validate` rejects these identifier_types with the helpful message "identifier_type X not supported in M2; manual playbook procedure applies — see dsar-operator-playbook.md §Per-surface manual fallback"; (c) D5 row stays OPEN with trigger = first prod DSAR with phone/name+address identifier_type. Calendar backstop: 2026-08-22 (D2/D3/D4 currency review pair) — at backstop, EITHER ship walker (re-scope into a new milestone) OR confirm "no trigger fired; manual procedure remains " + extend trigger another 90d. |
| 🆕 **F-DSAR31** (added 2026-05-25 per M2 Sprint B scoping decision — `m2-design-2026-05-25.md` §3.2 absorption descope) | **session-summaries surface NOT walked in M2 — requires tenant_hash discovery.** Sprint A design §3.2 absorbed `picasso-session-summaries-staging` + `picasso-session-events-staging` into M2 done-bar #1 as new walkers, reasoning that both surfaces share the same psid→sessionIds subject-resolution input. Sprint B implementation discovered an asymmetry: **session-events is sessionId-keyed (`pk=SESSION#{sessionId}, sk=STEP#{n}`) — reachable directly from the psid resolver output. session-summaries is tenant_hash-keyed (`pk=TENANT#{tenant_hash}, sk=SESSION#{sessionId}`) — requires resolving `tenant_id → tenant_hash` before any Query can run.** The tenant_hash (e.g. `my87674d777bf9`) is a different identifier than tenant_id; Master_Function_Staging computes it but it's not currently exposed to the DSAR Lambda's environment or input event. Sprint B descoped session-summaries to preserve simplicity (Karpathy #2 — minimum code that solves the problem; #3 — surgical changes; #4 — verifiable success criteria). | `picasso-session-summaries-staging` (pk=TENANT#{tenant_hash}, sk=SESSION#{sessionId}); ItemCount=30 in staging acct 525 at Sprint A read 2026-05-25. Discovery context: M2 Sprint B implementation 2026-05-25 (lambda#157). | n/a (capability scope decision; downstream privacy impact bounded by recent-messages reaching same Meta session content with 7-day TTL — session-summaries persists longer per the source-archiver pattern but the rows are session-level aggregates not raw consumer messages) | L / M (likelihood LOW — recent-messages reaches the primary Meta content with 7d TTL; session-summaries persistence is the long-tail rows ≤ 365d. impact MEDIUM if a subject's Meta data persisted long enough to age out of recent-messages but stayed in session-summaries — DSAR walker would miss those rows.) | M3 playbook §"Per-surface manual fallback" entry added in Sprint E: operator runs `aws dynamodb query --table-name picasso-session-summaries-staging --key-condition-expression "pk = :pk AND begins_with(sk, :sk)" --expression-attribute-values '{":pk":{"S":"TENANT#<TENANT_HASH>"},":sk":{"S":"SESSION#meta:"}}'` after manually deriving tenant_hash via tenant-registry lookup. Recent-messages walker is the primary surface; session-summaries is the long-tail backstop. | Walker for session-summaries not yet implemented. Operator must manually derive tenant_hash. | **Chris** | **Sprint E ships (or M2 follow-on)**: choose between (a) **operator-passed tenant_hash field** on the DSAR event (contract change — small Lambda mod + playbook update); (b) **tenant_id → tenant_hash lookup via tenant-registry** (new code path; requires either tenant-registry table read OR a single-table-design lookup); (c) **defer-with-observable-trigger**. Recommendation: (c) for current product scale (recent-messages 7d TTL covers most consumer DSARs). **OBSERVABLE TRIGGER REVISION (audit row #11, 2026-05-25)**: the original "first DSAR where data missed" trigger was non-observable — operator cannot know the gap without the walker that's being deferred. **New trigger framing**: (1) **Quarterly sentinel check at every D2/D3/D4 currency review** (next 2026-08-22): operator runs `aws dynamodb scan` against `picasso-session-summaries-staging` for the 5 most-recently-closed DSARs (sample from `dsar-log.md`) — if ANY scan finds rows matching the subject's tenant + Meta-session prefix that recent-messages walker did NOT export, promote to option (a) or (b) immediately. (2) **Atlanta tenant LOI** OR **any new tenant onboarding** triggers manual session-summaries posture check before tenant goes live. (3) **Annual calendar backstop** 2027-05-25 (one year from this row's revision) regardless. At each trigger, EITHER ship session-summaries walker OR record empirically-verified "recent-messages coverage acceptable" + extend trigger window. |
| 🆕 **F-DSAR32** (added 2026-05-25 per M2 Sprints A+B+C phase-completion-audit row #6 — tech-lead-reviewer 🟡) | **ARCHIVE walker access path returns keys only — not object bodies.** M2 Sprint C `_walk_archive_bucket` deliberately omits `s3:GetObject` from the DSAR Lambda IAM grants (documented in `infra/modules/lambda-pii-dsar-staging/main.tf` DELIBERATE ABSENCES comment). On `request_type='access'`, the walker returns `exported_keys` (list of S3 keys) — not the object contents. The rationale: keep Lambda response < 6 MB; operator pulls bodies via `aws s3 cp` under own SSO role. **Asymmetry**: M1 done-bar #5 established that the access path returns "subject-scoped JSON export equivalent to the delete path" for DDB-keyed surfaces (form-submissions, recent-messages, etc.). For archive content, the access path is no longer equivalent — operator-mediated S3 fetch is an extra step the subject's response is contingent on. CCPA §1798.110 (right to know) + GDPR Art 15 (right of access) require the controller to provide the data; operator-side fetch is acceptable if documented procedure exists, but the playbook entry for it is not yet written. | `picasso-archive-staging/sessions/{sessionId}/*` (current scope: staging-only; F-DSAR17 routes prod analogue when promoted). Walker source: `lambda_function.py:_walk_archive_bucket` (access branch returns `exported_keys`). IaC: `infra/modules/lambda-pii-dsar-staging/main.tf` `ArchiveBucketListVersions` + `ArchiveBucketDeleteVersions` SIDs (no Get* grant). Discovery context: M2 phase-completion-audit 2026-05-25 (tech-lead-reviewer finding #6). | n/a (response-completeness gap; downstream consumer-rights impact is procedural, not infrastructural) | L / M (likelihood LOW — staging-only today; only consumer DSARs with archived Meta content would fire; impact MEDIUM if a regulator-class subject access request requires the controller to deliver complete data without operator-side post-processing) | Sprint E playbook entry: operator-mediated `aws s3 cp s3://picasso-archive-staging/{key} -` per returned key, with object content placed in the subject's response packet. The walker's `exported_keys` field IS the actionable input. Existing operator-attended fulfillment posture (M3 playbook) covers the equivalent procedure for the DDB surfaces. | Lambda returns keys not bodies; operator must perform N+1 fetch step. No automated subject-response packet today. | **Chris** | **Sprint E ships**: (a) M3 playbook §"ARCHIVE access fulfillment" entry — copy-pasteable `aws s3 cp` loop over the Lambda's `exported_keys["archive"]` field; output packaged into subject response with the DDB-surface exports. (b) Alternative: add `s3:GetObject` to the ARCHIVE_BUCKET grants + extend walker to inline object bodies for access requests (~30min code + IaC). Recommendation: (a) for current scale (staging-only; few archive objects expected per subject); promote to (b) on first prod-archived consumer DSAR + response size proves manageable. Calendar backstop: 2026-08-22 (D2/D3/D4 currency review pair). |
| 🟡 **F-DSAR33 CLOSING 2026-05-26** via option (a) — companion lambda#163 + picasso#242 ship the BSH dual-emit + restored alarms; backstop 2026-08-22 becomes obsolete on apply (originally added 2026-05-26 per staging apply unblock — picasso#236+#237 surface) | **PII subject-index EMF metrics emit with `TenantId` dimension but no operator-facing alarm.** Sprint E2 added CloudWatch Embedded Metric Format emission in BSH `pii_subject.js` (`PII/SubjectIndex.IndexRaceUnresolved` + `.IndexUnavailable`); Sprint F3 / audit-of-audit finding 2 attempted to add alarms via `metric_query { expression = "SUM(SEARCH(...))" }` to aggregate across tenants. **AWS limit discovered 2026-05-26 staging apply**: `ValidationError: SEARCH is not supported on Metric Alarms` — SEARCH expressions are dashboard-only. Alarms removed (picasso#237) to unblock IaC apply. **Metrics still emit** to `PII/SubjectIndex` namespace but are un-alarmed. | BSH `pii_subject.js` EMF emit (TenantId dimension); `infra/modules/lambda-bedrock-handler-staging/main.tf` (alarms removed; comment block preserves redesign options). Discovery context: M2 phase-completion-audit picasso#228 merge → staging apply 2026-05-26T04:48:24Z. | n/a (observability gap) | L / M (likelihood LOW — EMF metrics still emit + CW Logs Insights queries can still locate failures; impact MEDIUM if a silent IndexRaceUnresolved storm hides a subject-index data-loss event) | **Three redesign options enumerated in IaC comment**: (a) BSH Lambda emits the SubjectIndex metrics a SECOND time with NO `TenantId` dimension → simple plain alarm on the no-dim series (~1h Lambda + ~30min IaC). (b) Per-tenant alarms via `for_each` over tenant list (high cardinality at scale; fine for current solo-operator setup). (c) Lambda-based custom-metric aggregator (over-engineered). | Metrics emit but no operator-facing alarm. CW Logs Insights queries on the EMF metric still work but require manual operator action. | **Chris** | **Sprint E ships (or M2 follow-on)**: choose between (a)/(b)/(c). Recommendation: (a) for current scale — single-line Lambda change emits the metric twice (once with TenantId for per-tenant dashboards, once without for the alarm). **Calendar backstop: 2026-08-22 (D2/D3/D4 currency review pair).** At backstop, EITHER ship option (a) OR record "no Index failure observed in CW Logs; defer another 90d with rationale" in master plan revision history. |
| 🆕 **F-DSAR34** (added 2026-05-26 with M2 Sprint D walker + writer ship) | **Per-tenant S3 fulfillment buckets are IAM-gated, not always-granted.** Sprint D shipped the walker (lambda#164) + writer (lambda#166) for the per-tenant `fulfillment_path` deletion path. The DSAR Lambda role's policy was deliberately left WITHOUT bucket-scoped grants (`infra/modules/lambda-pii-dsar-staging/main.tf` deliberate-absences) because empirical tenant config scan 2026-05-26 found **zero** tenants currently configure `fulfillment.type='s3'` (ATL642715, AUS123957, FOS402334, MYR384719 all default to email). Walker hits AccessDenied on any unbranded `(bucket, tenant_id)` pair → surfaces in `failed_paths` + manual followup. Picasso #N (this PR) wires the `var.fulfillment_grants` scaffold (default `[]`; dynamic statement renders zero policy lines today). | DSAR Lambda IAM role `picasso-pii-dsar-staging-role`; walker `_walk_fulfillment_s3` in `lambda_function.py`; writer `_persist_fulfillment_path` (MFS) + `persistFulfillmentPath` (BSH). Empirical scan artifacts: `/tmp/tenant-fulfillment-scan/*.json` (5 configs; 0 hits). | n/a (gate-trigger gap; not data-exposure) | L / L (likelihood LOW — no tenant uses S3 fulfillment today; impact LOW because failure mode is operator-visible manual followup, not silent data loss — walker emits `failed_paths` + audit event) | Operator-side: when a tenant enables S3 fulfillment, also append `{bucket=..., tenant_id=...}` to `var.fulfillment_grants` in `infra/main.tf` (tracked in `dsar-operator-playbook.md` §F-DSAR34). IAM module's dynamic statement renders one Allow per pair; `terraform apply` is the gate. | No automated detection that a tenant has enabled S3 fulfillment without operator-side adding the grant — relies on the operator pairing the two changes. Tenant onboarding workflow should make this paired action a checklist item. | **Chris** | **Operational discipline (no code ship needed for closure)**: each new S3-fulfillment tenant gets the grant added in the same picasso IaC PR that ships the tenant config change. Watch trigger to promote severity: first time a real DSAR fires `failed_paths` for a `(bucket, tenant_id)` pair the operator didn't pre-grant. Calendar backstop: **2026-08-22** (D2/D3/D4 currency review) — at backstop, IF no tenant has yet enabled S3 fulfillment, this row stays open (cost = zero) until first enablement; IF any tenant has enabled it, verify their grant is in the IaC. |
| ✅ **F-DSAR18 CLOSED 2026-05-24** (originally added 2026-05-23 M4 functional verification; tracked through M1.G6 ship) | **Active widget chat-form writer `Bedrock_Streaming_Handler_Staging/form_handler.js` did not write `pii_subject_id`** — empirically verified 2026-05-23 against staging row `form_contact_1779557864165` (`pii_subject_id: null`). DSAR walker `_walk_form_submissions` FilterExpression silently false-negatived every BSH-written row. **Closure 2026-05-24** via M1.G6 ship: lambda PR #148 (BSH `pii_subject.js` Node.js port + form_handler.js wire-in + 28 tests) + picasso PR #186 (IAM grant + `PII_SUBJECT_INDEX_TABLE` env var). Functional verification 04:25-04:26Z 2026-05-24: 2× real form submissions same email returned matching `pii_subject_id = psub_2d88ed3bc9f14f8f8946c19552caca85`; index entry exists in `picasso-pii-subject-index-staging`. | `picasso-form-submissions-staging` (D2 §B; D4 Tier 3); BSH `form_handler.js` Item dict now writes `pii_subject_id` (lambda PR #148). Walker `_walk_form_submissions` Filter now returns BSH-written rows correctly. Closure refs: lambda PR #148 + picasso PR #186 (merged 04:13-04:14Z 2026-05-24). | 3 (data) | L / L (residual: only pre-M1.G6 historical rows have null `pii_subject_id`; tracked separately under F-DSAR1 / M1.G4 backfill trigger condition) | ✅ Closed. **New primary control:** BSH `pii_subject.js` get-or-create indexed lookup (behavioral parity with Python writer). Operator `manual_followup` fallback retained for pre-M1.G6 historical rows via F-DSAR1. End-to-end walker automation restored for active writer path. | ✅ Closed for active-writer rows post-2026-05-24T04:20:43Z. Historical pre-M1.G6 rows still UNINDEXED — tracked under F-DSAR1 / M1.G4 with their own backfill trigger conditions. | **Chris** | ✅ **CLOSED** via M1.G6 ship — option 1 chosen + executed (Node.js port of Python writer pattern). 28 new pii_subject.js unit tests guard behavioral parity; 1 form_handler jest test guards the wire-in. Master plan v0.14 records closure. |

---

## Operational fulfillment workflow (per `compliance-implementation-advisor` review, 2026-05-20)

This section is the **how**: what the operator (Chris) actually does when a request arrives. Designed for zero-volume / solo-founder scale. Operational posture = `Gmail + AWS CLI + DSAR Lambda + 6 markdown templates`. No custom UI; no ticketing system; no portal.

### Workflow (numbered, end-to-end)

1. **Intake.** `privacy@myrecruiter.ai` alias → Chris's Gmail. Filter routes inbound mail to label `dsar/open`. Published in tenant DPAs + consumer-facing widget privacy notice + marketing site footer. Mailed letters scanned into the same Gmail thread.
2. **Triage.** Open a new row in [`dsar-log.md`](./dsar-log.md). Decide: is this a DSAR? Which right? Which jurisdiction? Which tenant? Authorized agent (counsel-for-subject)?
3. **Verification.** Per [`dsar-verification-posture.md`](./dsar-verification-posture.md) — interim "reply-from-on-file-email + one corroborating fact for mismatches" posture (counsel-reviewed at trigger).
4. **Execution.** `aws lambda invoke` with payload `{subject_identifier, identifier_type, request_type, tenant_id, operator, dsar_id, dry_run}`. Lambda dry-runs by default; operator reviews `rows_touched` preview; re-invokes with `dry_run=false`.
5. **Manual-followup tickets.** Lambda returns a `manual_followups` array. For Finding 9 (tenant sinks): operator emails tenant admin using [`templates/tenant-sink-deletion-request.md`](./templates/tenant-sink-deletion-request.md). For Finding 12 (PSID): tenant-scoped DDB Scan or operator gets PSID from Meta Business Suite. For Finding 14 (ARCHIVE_BUCKET): operator runs the recorded reachability procedure from [`archive-reachability-decision.md`](./archive-reachability-decision.md).
6. **Response to subject.** Gmail reply on the same thread with one of [`templates/dsar-response-access.md`](./templates/dsar-response-access.md), [`templates/dsar-response-delete.md`](./templates/dsar-response-delete.md), or [`templates/dsar-response-no-record.md`](./templates/dsar-response-no-record.md). JSON attachment for access / export.
7. **Audit.** Three layers: Lambda's audit row in `picasso-pii-dsar-audit-staging` + `dsar-log.md` row (closed status) + Gmail thread retained under label `dsar/closed`.
8. **SLA mechanics.** EventBridge → SNS → email alarm fires at intake+25d for any open row. Google Calendar belt-and-suspenders reminders at intake+21d / intake+38d.
9. **Escalation triggers (engage counsel).** Subject contests verification; novel authorized-agent claim; cross-border element; tenant refuses Finding-9 sink deletion; subject asserts model-training; controller-vs-processor conflict; 3+ concurrent open DSARs.

### Day-1 artifacts (what must exist for the workflow to be operational)

| Artifact | Type | Status |
|---|---|---|
| **`picasso-pii-dsar-staging` Lambda + IaC** — operator-invocable; takes `{subject_identifier, identifier_type, request_type, tenant_id, operator, dsar_id, dry_run}`; walks D2, executes via Apply-1 IAM role, writes audit row, returns rows-touched + manual-followups | Code | **Next** — single Path A engineering deliverable (post-Phase-0.5) |
| **6 markdown templates** — `dsar-response-{access,delete,no-record}.md` + `dsar-verification-request.md` + `dsar-refusal.md` + `dsar-extension-notice.md` + `tenant-sink-deletion-request.md` | Docs | **Phase 0.5 / this PR** |
| **`dsar-log.md`** — flat ledger schema; one row per request | Docs | **Phase 0.5 / this PR** |
| **`dsar-verification-posture.md`** — interim verification standing language; counsel-reviewed at trigger | Docs | **Phase 0.5 / this PR** |
| **`archive-reachability-decision.md`** — one-shot CLI verification result for `ARCHIVE_BUCKET` posture | Docs (records CLI output) | **Phase 0.5 / this PR** |
| **`bedrock-invocation-logging-decision.md`** — one-shot CLI verification result for account-level Bedrock logging | Docs (records CLI output) | **Phase 0.5 / this PR** |
| **EventBridge SLA alarm** (G-D enforcer) — scans `picasso-pii-dsar-audit-staging` daily for open rows nearing SLA; SNS → email | Infra | **Next** (alongside Lambda) |
| **Gmail `privacy@` alias + `dsar/{open,awaiting-verification,closed}` labels + filter** | Operator config | **Next** (Chris sets up directly; no repo artifact) |

### What this changes about counsel triggers

The fulfillment workflow runs **without counsel**. Counsel is the escalation path (workflow §9), not the routine path. Engaging counsel pre-trigger remains process theater; engaging on the escalation conditions above is the right use of counsel hours.

---

## Cross-cutting horizontal concerns (named here so they aren't lost; not separate rows — they are facets that bear on G-A / G-G / G-I / F9 above)

- **Vulnerable-population content class** (foster/adoption, DV shelters, hospice, recovery; Austin Angels reference tenant). Form-data free-text from these tenants may contain disclosures about minors, crisis, health, housing, immigration even when no field is labeled as such. **Affects every Tier 3 row in this register** by elevating the impact column from "M/H" toward "H/H" in practice. D5 records this here rather than as a 16th row because no single surface owns it — it is a property of the tenant population.
- **Donor confidentiality vs sensitive-content (D4 rule 8).** Donor-class data is Tier 3 by confidentiality even when payload carries no sensitive-content. D5 row F9 carries both framings; the mitigations differ (sensitive-content = redact + restrict; confidentiality = pseudonymize + audit access).
- **Tier 3 controls vs current posture (D4 Tier-vs-lifecycle gaps subsection).** The strategy doc's Tier 3 controls ("redact logs; restrict access; shorter retention; human handoff rules") are NOT currently met on the Tier 3 surfaces (no log redaction; no CMK on Tier 3 tables; long retention on form-submissions). This is distributed across rows G-A (logs), G-C (retention), F11 (encryption).
- **Recipient-list inclusion of staff emails in tenant config** = operator PII flowing to operator readers (D3 §3). Folded into G-H scope (operator-PII rights determination).

---

## Verification (mechanical exit-criteria check)

- ✅ **Each G-A..G-K gap has a row** (11 rows).
- ✅ **Row count: 18 rows** (11 G-rows + 4 F-rows seeded at Phase 0.5 + 2 F-DSAR rows added 2026-05-20 from DSAR Lambda item-1a audit + 1 F-DSAR row added 2026-05-21 from 2-advisor audit of PR #135). Original ≤ 15 cap superseded once theoretical gaps graduated to operational on Lambda landing.
- ✅ **No unmitigated H/H row lacks a named owner:**
  - G-A H/H → Path B
  - G-I H/H → Counsel-pending
  - F9 H/H → Chris + Counsel-pending
  - F-DSAR1 L/I = M/M today (expected frequency low); H/H by impact when triggered — owner Chris.
  - F-DSAR2 L/I = L/H — owner Chris; documented residual risk with named revisit triggers.
  - F-DSAR3 L/I = M/M — owner Chris; walker mitigates today; durable fix deferred to writer-side normalization.
  - F-DSAR4 L/I = M/M today (low DSAR frequency × narrow 0-72h structural-exposure window); H/H by impact when a chat-only-within-72h subject DSAR arrives. Owner Chris; durable fix = MFS writer subject-linkage emission, batched with F-DSAR3 sweep.
- ✅ **Closed rows preserved as audit trace** (G-B, G-E).
- ✅ **All columns populated** for every row.
- ✅ **No new policy invention** — D5 records risks + named target actions; specific mitigation design lives in Step 10 / Apply-2 / second-wave.
- ✅ **No Decision A or B pre-decisions** — F9 explicitly flags Decision A as input at Step 10.

## What this register feeds

- **Step 10 (re-baseline `CONSUMER_PII_REMEDIATION.md`)** consumes this register as **the prioritization input**. Path A phases re-ordered to address H/H rows first (G-A via Path B coordination; G-I via counsel; F9 via Decision A). Rows G-B and G-E are closed; rows G-C, G-J, F11, F12 inform Apply-2 preconditions.
- **Step 11 (Phase 0.5 closeout)** records (a) which gaps closed in Phase 0.5 (G-B, G-E); (b) which rows have new named owners (all of them); (c) which rows are counsel-pending (G-G, G-H, G-I + supplementaries); (d) the named **next concrete Path A action** that this register surfaces.
- **Counsel response (when it arrives)** will update G-G / G-H / G-I + the supplementary rows tied to S1–S5 in PR #153. Re-baseline + closeout reference this register; the register itself is updated in a small focused PR when counsel responds.

## Open items deferred from D5

| Item | Why deferred | Where it goes |
|---|---|---|
| Per-tenant vendor inventory (second-wave) | Out of Phase 0.5 scope per plan; named in Step 11 closeout. | Second-wave deliverable. |
| Live-state reconciliation of D2 inventory vs runtime AWS | Apply-2 precondition; Phase 0.5 is IaC-truth. | Apply-2 design gate. |
| `ARCHIVE_BUCKET` posture (name/region/encryption/lifecycle) | D3 open item; requires live AWS inspection. | Apply-2 precondition or operational verification. |
| F14 archive-bucket retention design (delete-reachability) | Decision A input at Step 10. | Step 10. |

---

## Links

- Plan: `~/.claude/plans/let-s-work-on-the-cheerful-manatee.md` (Step 9)
- D1 charter: [`pii-project-charter.md`](./pii-project-charter.md)
- D2 inventory: [`pii-inventory.md`](./pii-inventory.md) — Findings 1–13
- D3 flow map: [`data-flow-map.md`](./data-flow-map.md) — Findings 14–16 + horizontal concerns
- D4 classification: [`data-classification.md`](./data-classification.md) — Tier-vs-lifecycle gaps + rule 8
- Counsel package: [`counsel-input-package.md`](./counsel-input-package.md) — Q1/Q2/Q3 + S1–S5
- Plan-gaps review (G-A..G-K origin): `memory/project_consumer_pii_remediation_path_a_plan_gaps_review_2026-05-19.md`
- Step 5 widget-claim verification (G-A evidence): `memory/project_pii_governance_phase05_step5_widget_claim_verification_2026-05-20.md`
