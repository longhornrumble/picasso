def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "headers": {"Content-Type": "text/plain"},
        "body": "placeholder — real code deploys via PR A2 (analytics_writer integration)",
    }
